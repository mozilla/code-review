---
pull_request_rules:
  - name: Automatic merge on approval ✔
    conditions:
      - "#approved-reviews-by>=1"
      - "status-success=Community-TC (pull_request)"
    actions:
      merge:
        method: squash
